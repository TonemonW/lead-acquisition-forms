{
    "name": "lead-acquisition-forms-monorepo",
    "private": true,
    "workspaces": [
        "frontend",
        "backend",
        "shared"
    ],
    "scripts": {
        "build:shared": "npm run -w @lead/shared build",
        "build:frontend": "npm run build:shared && npm run -w frontend build:widget && npm run -w frontend build:iframe",
        "build:backend": "npm run build:shared && npm run -w backend build",
        "deploy:backend": "npm run -w backend deploy",
        "dev:frontend": "npm run -w frontend dev",
        "dev:backend": "npm run -w backend serve",
        "lint": "npm run -w frontend lint && npm run -w backend lint"
    },
    "dependencies": {
        "axios": "^1.11.0",
        "zod": "^3.23.8"
    }
}